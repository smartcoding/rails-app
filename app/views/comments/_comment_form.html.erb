<%= form_for([@post, @comment]) do |f| %>

  <%= render 'common/form_errors', object: @comment %>

  <p>
  <%= f.label :body %><br />
    <%= f.text_area :body %><br />
    <%= f.submit 'Add Comment' %>
  </p>

<% end %>
